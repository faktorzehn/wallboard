'use strict';

describe('Directive: analysis', function () {
  beforeEach(module('wallboardApp'));

  var element;

  it('should make hidden element visible', inject(function ($rootScope, $compile) {
   // element = angular.element('<analysis></analysis>');
   // element = $compile(element)($rootScope);
  //  expect(element.text()).toBe('this is the analysis directive');
  }));
});
